    <h1>{{pageTitle}}</h1>
    <ul>
        {{#each cart}}
            <li>{{this.title}} - {{this.description}} - ${{this.price}}</li>
        {{/each}}
    </ul>


<script src="/js/agregarproducto.js"></script>
